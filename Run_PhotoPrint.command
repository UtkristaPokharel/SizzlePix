#!/bin/bash
# Move to the directory where this script is located
cd "$(dirname "$0")"

echo "🚀 Starting PhotoPrint..."
echo "-----------------------------------"

# Run the root start script
npm start
